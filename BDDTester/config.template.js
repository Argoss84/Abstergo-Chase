window.APP_CONFIG = {
  apiBaseUrl: "${API_BASE_URL}",
  cognitoDomain: "${COGNITO_DOMAIN}",
  cognitoClientId: "${COGNITO_CLIENT_ID}",
  cognitoRedirectUri: "${COGNITO_REDIRECT_URI}",
  cognitoScope: "${COGNITO_SCOPE}"
};
